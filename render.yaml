services:
  - type: web
    name: real-estate-api
    runtime: docker
    dockerfilePath: Dockerfile.api
    envVars:
      - key: PYTHONPATH
        value: .
      - key: DATALAKE_ROOT
        value: data
